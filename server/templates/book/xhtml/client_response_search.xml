<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">

<head profile="http://www.w3.org/1999/xhtml/vocab">
    <title>Search results for: 
    {% if title.query %}
        Query "{{ title.query }}";
    {% endif %}
    {% if title.tit %}
        Title "{{ title.tit }}";
    {% endif %} 
    {% if title.author %}
        Author "{{ title.author }}";
    {% endif %}
    {% if title.lang %}
        Language "{{ title.lang }}";
    {% endif %}
    {% if title.tag %}
        Tag "{{ title.tag }}";
    {% endif %}
    </title>
  
  <meta http-equiv="content-type" content="application/xhtml+xml; charset=utf-8" />
  <meta http-equiv="X-UA-Compatible" content="chrome=1" />
  
  <!-- Links --> 
  <link rel="search" type="application/opensearchdescription+xml" title="Opensearch description" href="opensearch" />

  <!-- CSS 
  <link rel="stylesheet" href="/stylesheets/screen.css" type="text/css" media="screen, projection" /> 
  <link rel="stylesheet" href="/stylesheets/print.css" type="text/css" media="print" /> 
  <link href="/stylesheets/normal.css?1255441690" media="screen" rel="stylesheet" type="text/css" />
  -->
  
</head>


<body>

<div id="header">
  
    <div class="container">
        <div class="span-19">
            <h1 class="fb-blue bottom">Search results for: </h1>
            <form name="test" action="../../search">
              Query: <input type="text" name="query" value="{{ title.query }}">
            <!--  <input type="text" name="author"-->
              <input type="submit" value="Search" >
            </form><br/>

<!--            <h2 class="gray">
                {% if title.query %}
                    Query "{{ title.query }}";
                {% endif %}
                {% if title.tit %}
                    Title "{{ title.tit }}";
                {% endif %} 
                {% if title.author %}
                    Author "{{ title.author }}";
                {% endif %}
                {% if title.lang %}
                    Language "{{ title.lang }}";
                {% endif %}
                {% if title.tag %}
                    Tag "{{ title.tag }}";
                {% endif %}
            </h2>
-->        </div>
    </div>

{% for book in books %}
<div align="left">
<hr class="space" />

      {% if not book.book_file.all.0.img_link %}
        <a href="book/id{{ book.id }}"><img alt="{{ book.title }}" class="cover" width="120" height="180" hspace="10" src="/pic/nobookcover" title="{{ book.title }}" /></a>
      {% else %}
        <a href="book/id{{ book.id }}"><img alt="{{ book.title }}" class="cover" width="120" height="180" hspace="10"  src="{{book.book_file.all.0.img_link}}" title="{{ book.title }}" /></a> 
      {% endif %}  






    <div class="float-right small">
        {% for tag in book.tag.all %}
            <category term="{{ tag }}"/>
        {% endfor %}
    </div>

    
    <h3 class="fb-blue small-bottom-margin"><a href="book/id{{ book.id }}">{{ book.title }}</a></h3>
    <h4 class="charcoal">by 
      {% for author in book.author_set.all %}
         <em><a href="author/id{{ author.id }}" class="gray">{{ author.name }}</a></em>
      {% endfor %}
    </h4>    
    {% for annotation in book.annotation.all %}
            <annotation> {{ annotation }} </annotation>
    {% endfor %}
  </div>

    
      

{% endfor %}


<!--    <link href="search.atom?" rel="alternate"></link>
    <link href="search.atom?items_per_page={{items_per_page}};query={{ query }}" rel="self"></link>
    <link rel="next" href="search.atom?page={{next}};items_per_page={{items_per_page}};query={{ query }}"/>
-->    

<div class="pagination">

{% for ind in seq%}
    {% ifequal ind curr %}
        <span class="current">{{curr}}</span>
    {% else %}
        {% if query %}
            <a href="search?page={{ind}};items_per_page={{items_per_page}};query={{ query }}" rel="next">{{ind}}</a>
        {% else %}        
            {% if author and title %}
                <a href="search?page={{ind}};items_per_page={{items_per_page}};author={{ author }};title={{ title }}" rel="next">{{ind}}</a>
            {% else %}
                {% if not author and not title %}
                    <a href="all?page={{ind}};items_per_page={{items_per_page}}" rel="next">{{ind}}</a>
                {% else %}    
                    {% if title %}
                        <a href="search?page={{ind}};items_per_page={{items_per_page}};title={{ title }}" rel="next">{{ind}}</a>
                    {% endif %}
                    {% if author %}
                        <a href="search?page={{ind}};items_per_page={{items_per_page}};author={{ author }}" rel="next">{{ind}}</a>
                    {% endif %}
                {% endif %}
            {% endif %}
        {% endif %}
    {% endifequal %}
{%endfor%}
</div>

</body>
</html>

